#include <stdio.h>

int main()
{
    char a[50];
    int i = 0, c = 0;

    printf("Enter a string: ");
    scanf("%s", a);  

    while (a[i] != '\0')
    {
        i++;
        c++;
    }

    printf("The length of string is %d.", c);

    return 0;
}
